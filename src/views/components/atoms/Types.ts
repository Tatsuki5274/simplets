export type SelectLabel = {
  value: string;
  label: string;
};

export type Superior = {
  email: string | null;
  username: string | null;
};

export type LinkType = {
  label: string;
  dest: string;
};
